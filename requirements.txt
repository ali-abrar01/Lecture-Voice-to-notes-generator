# minimal requirements determined by scanning project imports
# Only third-party packages that are actually imported in the codebase

Flask==3.0.0                # web framework (includes Jinja2, Werkzeug, etc.)
python-dotenv==0.21.1       # environment variable loader (.env support)
requests==2.31.0            # HTTP client used for API calls
reportlab==4.1.0            # PDF generation library

